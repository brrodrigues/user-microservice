eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: ${EUREKA_SERVERS:http://localhost:8756}
spring:
  application:
    name: user-microservice
debug: true
hystrix:
  metrics:
    enabled: true


